# 舆情审核机器人 (shenhe-bot)

舆情监控与AI审核机器人，实现三级预警（红/黄/绿），自动推送飞书。

## 快速开始

1. 复制 `.env.example` 为 `.env`，填入真实密钥
2. 复制 `config.json.example` 为 `config.json`，填入关键词
3. `pip install -r requirements.txt`
4. `python SHbot.py`